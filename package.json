{
  "dependencies": {
    "concurrently": "^3.5.0"
  },
  "scripts": {
    "deploy": "cd front-end && ng build --aot -prod -op ../back-end/dist && cd ..",
    "start": "cd back-end && node server.js"
  }
}
